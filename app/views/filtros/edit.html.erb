<% if current_user && current_user.AdminStatus? %>

<h1>Editing Filtro</h1>

<%= render 'form', filtro: @filtro %>

<%= link_to 'Show', @filtro %> |
<%= link_to 'Back', filtros_path %>
<% else %>
<%= render 'layouts/error' %>
<% end %>
