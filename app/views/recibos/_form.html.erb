<%= form_for(recibo) do |f| %>
  <% if recibo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recibo.errors.count, "error") %> prohibited this recibo from being saved:</h2>

      <ul>
      <% recibo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :folio_recibo %>
    <%= f.number_field :folio_recibo %>
  </div>

  <div class="field">
    <%= f.label :kmanterior_recibo %>
    <%= f.text_field :kmanterior_recibo %>
  </div>

  <div class="field">
    <%= f.label :kmactual_recibo %>
    <%= f.text_field :kmactual_recibo %>
  </div>

  <div class="field">
    <%= f.label :combustible_recibo %>
    <%= f.text_field :combustible_recibo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
